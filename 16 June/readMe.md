### What is Function ?

=> Identified(named) block(group of code) of re-usable code.

### Types of functions :-

1.  In-built (Pre-Defined function)
2.  Custom (User Defined function)

Note :-
If you define a function that does not mean you have called it. if you want to execute it you need to call/invoke it explicitely.
